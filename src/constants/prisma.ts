/**
 * Prisma Query Modes
 */
export const PRISMA_QUERY_MODE = {
  INSENSITIVE: "insensitive" as const,
  DEFAULT: "default" as const,
} as const;

/**
 * Prisma Sort Orders
 */
export const SORT_ORDER = {
  ASC: "asc" as const,
  DESC: "desc" as const,
} as const;
